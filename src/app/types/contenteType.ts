type ImageBento = {
  name: string,
  author: string,
  url: string
}

type ArticleList = {
  title: string,
  author: string,
  teaser: string,
  url: string
}

type EventList = {
  title: string,
  date: string,
  url: string
}

export type {ImageBento, ArticleList, EventList}